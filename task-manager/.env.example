# M365 credentials — copy to .env and fill in (Pattern 1: never commit .env)
# Required only for Outlook integration. Leave blank for standalone mode.
M365_CLIENT_ID=
M365_TENANT_ID=
M365_CLIENT_SECRET=
